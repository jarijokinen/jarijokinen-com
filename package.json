{
  "private": true,
  "name": "jarijokinen-com",
  "version": "0.0.1",
  "scripts": {
    "assets:build": "run-s css:build js:build",
    "assets:watch": "run-p css:watch js:watch",
    "astro:build": "astro build",
    "astro:dev": "astro dev",
    "build": "run-s assets:build astro:build",
    "css:build": "./scripts/sass.sh",
    "css:watch": "./scripts/sass.sh --watch",
    "dev": "run-p assets:watch astro:dev",
    "js:build": "./scripts/esbuild.sh --minify",
    "js:watch": "./scripts/esbuild.sh --watch"
  },
  "type": "module",
  "devDependencies": {
    "@astrojs/sitemap": "^3.7.4",
    "astro": "^7.3.4",
    "npm-run-all": "^4.1.5",
    "sass": "^1.105.0"
  }
}
